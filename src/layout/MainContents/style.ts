import styled from 'styled-components';
import { HeaderHeight } from '../../constant/HeaderHeight';

const MainContents = styled.main`
  padding-top: ${HeaderHeight.LogoArea + HeaderHeight.OptionBar + HeaderHeight.Border}px;
`;

export { MainContents };
